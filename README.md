# Nix

This is my nixos config for hyprland

## Apply

Full system (needs sudo): `nh os switch`

Home only, no sudo (shares `modules/home` with the system config): `nh home switch --backup-extension backup`
